
# This script was generated by the train.py script in EHA's private eval-scripts
# repository.

import numpy as np
from numpy import array, int32
base_classifier = {'C': 1.0,
 'class_weight': None,
 'classes_': array([False,  True], dtype=bool),
 'coef_': array([[ 0.36945455,  0.00772055, -0.0168576 ,  0.29789766,  0.49660641,
         1.02399585, -0.00263311, -0.57737153, -0.55397861, -0.2825381 ,
         0.        ,  0.        ,  0.        ,  0.        ,  0.        ]]),
 'dual': False,
 'fit_intercept': True,
 'intercept_': array([-8.37127805]),
 'intercept_scaling': 1,
 'max_iter': 100,
 'multi_class': 'ovr',
 'n_iter_': 31,
 'penalty': 'l2',
 'random_state': None,
 'solver': 'liblinear',
 'tol': 0.0001,
 'verbose': 0}
HIGH_CONFIDENCE_THRESHOLD = 0.5
GEONAME_SCORE_THRESHOLD = 0.2
contextual_classifier = {'C': 1.0,
 'class_weight': None,
 'classes_': array([False,  True], dtype=bool),
 'coef_': array([[ 0.26684763,  0.01009869, -0.08587608,  0.14395366,  0.11229759,
         0.21159864, -0.00170519, -0.72039338, -0.85025881,  0.18412953,
         0.93598429,  0.05542549,  1.07335631,  0.2378477 ,  1.50564854]]),
 'dual': False,
 'fit_intercept': True,
 'intercept_': array([-7.44308988]),
 'intercept_scaling': 1,
 'max_iter': 100,
 'multi_class': 'ovr',
 'n_iter_': 34,
 'penalty': 'l2',
 'random_state': None,
 'solver': 'liblinear',
 'tol': 0.0001,
 'verbose': 0}
# Logistic regression code from scipy
def predict_proba(X, classifier):
    """Probability estimation for OvR logistic regression.
    Positive class probabilities are computed as
    1. / (1. + np.exp(-classifier.decision_function(X)));
    multiclass is handled by normalizing that over all classes.
    """
    prob = np.dot(X, classifier['coef_'].T) + classifier['intercept_']
    prob = prob.ravel() if prob.shape[1] == 1 else prob
    prob *= -1
    np.exp(prob, prob)
    prob += 1
    np.reciprocal(prob, prob)
    if prob.ndim == 1:
        return np.vstack([1 - prob, prob]).T
    else:
        # OvR normalization, like LibLinear's predict_probability
        prob /= prob.sum(axis=1).reshape((prob.shape[0], -1))
        return prob
def predict_proba_base(X):
    return predict_proba(X, base_classifier)
def predict_proba_contextual(X):
    return predict_proba(X, contextual_classifier)
